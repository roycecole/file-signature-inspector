# 變更紀錄（Changelog）

檔案簽章鑑識台的功能演進紀錄。版本號是為了方便對照本文件自訂的，不對應任何正式發行版本。

---

## v3.7 — PWA、多執行緒掃描、Worker 池化、比較模式範圍可調、自訂簽章匯出入

### 新增
- **PWA（可安裝）**：新增 `manifest.json` 與 `service-worker.js`（cache-first 離線快取，版本化、自動清除舊快取）。**重要限制講在最前面**：Service Worker 只能在安全情境（https:// 或 http://localhost）下註冊，用 `file://` 直接開啟 `index.html`（這個工具原本、也持續支援的用法）不會觸發安裝——這是瀏覽器規格的限制，不是實作疏漏。註冊程式碼會偵測情境，不符合就靜默略過，不影響任何既有功能。想用到這個功能，把整個資料夾放到任何靜態網頁空間即可。
- **檔案分析搬進多執行緒**：新增 `js/detect-worker.js`，把原本在 `app.js` 裡的 `analyzeFile()` 核心邏輯抽成 `detectors.js` 的 `analyzeFileCore()`——一份程式碼，主執行緒直接呼叫、或丟給 Worker 池執行完全一樣，不需要維護兩份邏輯。`app.js` 新增 Worker 池（`detectWorkers[]`，數量依 `navigator.hardwareConcurrency` 決定，介於 1～4 顆）搭配輪詢分派，任何一個環節失敗都會自動退回主執行緒的 `analyzeFileCore()` 直接執行。自訂簽章新增/移除時會廣播給所有 Worker 保持同步（Worker 裡沒有 `localStorage`，沒同步就讀不到使用者自訂的簽章）。**這裡有件事值得說明**：抽出 `analyzeFileCore()` 之前，完全沒有任何測試直接驗證過「組裝完整結果」這條路徑——之前的測試都只分開測 `resolveSignature()`／`checkTrailing()`／`pdfRisks()` 這些片段，從沒驗證過組裝起來的最終物件對不對。這次補上了這個測試缺口。
- **SHA-256 也改用 Worker 池**：原本的單一雜湊 Worker 擴充成池化架構，任何一顆意外崩潰（`onerror`）都會自動補一顆新的頂上，盡量維持池子大小；`hashAll()`（「計算全部 SHA-256」）改用新的 `runWithConcurrency()` 限制同時進行數量、真正平行分派給多顆 Worker，而不是像原本那樣即使有多顆 Worker 可用，也因為逐一 `await` 而永遠只有一顆在忙。
- **比較模式的比對範圍可調整**：差異比對視窗新增下拉選單，可在 256 bytes／1 KB／4 KB／64 KB 之間切換，切換時只重新畫統計數字跟位元組格線，不會整個視窗重開、不會弄丟焦點位置；選擇會記住，同一次瀏覽階段下次開啟比較視窗沿用上次的範圍。
- **比較模式的鍵盤操作**：新增 <kbd>c</kbd> 快速鍵，把目前焦點所在的結果列加入／移出比較（最多兩筆），跟既有的 <kbd>r</kbd>（標記已檢查）、<kbd>u</kbd>（復原刪除）是同一套設計。
- **自訂簽章匯出／匯入**：自訂簽章區塊新增「匯出」「匯入」按鈕。匯出成 JSON 檔可以直接分享給同事，匯入時同名的簽章一律跳過、不覆蓋既有設定（避免不小心用舊檔案蓋掉剛調整好的定義），也接受單純的陣列格式（不一定要是本工具匯出的完整結構），並回報「新增幾筆、跳過重複幾筆、格式不符略過幾筆」。

### 測試
- 新增四組回歸測試（開發用，未隨專案分發）：
  - `detectworker_regress.js`：驗證抽出來的 `analyzeFileCore()` 組裝出的完整結果——PNG／執行檔偽裝／巨集 Office／空檔案／讀取錯誤——每種情況下最終物件的每個欄位都對，不只是判定邏輯片段。
  - `pwa_regress.js`：`manifest.json` 是合法 JSON 且含必要欄位；`service-worker.js` 裡要快取的檔案清單每一個都真的存在；反向檢查專案裡的核心檔案是否都有被排進快取清單（避免漏掉新檔案這種容易被忽略的錯誤）。
  - `pool_regress.js`：`runWithConcurrency()` 真的有限制住同時進行的工作數量、任務失敗不會卡住佇列；Worker 池其中一顆崩潰後會自動補一顆新的實例頂上。
  - `compare_cap_regress.js`：不同比對範圍下 `buildByteDiff()` 的行為正確，並確認重構時把 `COMPARE_CAP` 常數改名後沒有殘留（曾經是半成品狀態，這裡順便驗證修好了）。
- 既有六組回歸測試（含瀏覽器內建的 `test.html`，共 100 餘項案例）全數重跑確認互不影響。目前總計十組回歸測試。

## v3.6 — Web Worker 雜湊、差異比對、自訂簽章、JSON 匯出、主題跟隨系統

### 新增
- **SHA-256 搬進 Web Worker**：大檔案計算雜湊時不再卡住主執行緒畫面。`js/sha256-worker.js` 用 `importScripts()` 直接載入 `sha256.js`（不重複維護程式碼），`app.js` 新增 `sha256Async()` 包一層「Worker 優先、失敗自動退回主執行緒」的容錯——沒有 Worker、建構失敗、`postMessage` 失敗、Worker 回報錯誤、逾時（60 秒）都會安靜接住並退回主執行緒重算，不會讓使用者卡在無回應的狀態。已知限制：部分瀏覽器在頁面以 `file://` 開啟時不允許建立 Worker，這種情況會自動退回主執行緒，功能不受影響只是速度較慢。
- **拖曳中的即時項目計數**：拖曳檔案到拖放區上方時會即時顯示「準備加入 N 個項目」。**這裡要老實說明一個技術限制**：瀏覽器的拖放安全模型不允許在 `dragenter`／`dragover` 階段讀取被拖曳檔案的實際內容——`File` 物件只有在放開滑鼠、觸發 `drop` 事件之後才拿得到，這是各家瀏覽器刻意的安全限制，不是實作上的疏漏。所以無法在放開之前就顯示「其中有幾個是執行檔」，只能顯示項目數量；真正的格式判定跟現有流程一樣，在 drop 之後立刻進行。
- **差異比對模式**：每列新增「⇄ 加入比較」按鈕，選滿兩筆後畫面下方會出現浮動列，按下「比較這兩個檔案」開啟並排的位元組比較視窗（沿用跟明細面板一樣的雙色格線），顯示大小是否相同、SHA-256 是否相同（尚未計算時會提示）、前 256 bytes 中有幾個位元組不同與第一個差異位置。只比較前 256 bytes，不逐位元組比對整個大檔案——多數格式差異都落在檔案開頭，真要確認完全一致用 SHA-256 更快更準。
- **自訂簽章**：新增可摺疊的「自訂簽章」設定區塊，讓使用者輸入公司內部格式的名稱、副檔名、開頭 HEX 與類型，加入後這份工具立刻能辨識。設定存在 `localStorage`（key 為 `fsi-custom-sigs`），跟已檢查標記不同，這裡存的純粹是使用者輸入的格式定義、與檔案內容無關，所以不需要額外的隱私開關保護，預設就會記住。自訂簽章會跟內建簽章混在同一份依 magic 長度排序的清單裡比對，公平比長度。
- **JSON 匯出**：新增「匯出 JSON」按鈕，輸出結構化資料（含篩選條件、摘要統計、每筆檔案的完整判定結果與風險說明），方便串接其他工具或寫自己的後處理腳本。欄位用穩定的英文 key，格式名稱與判定結果同時提供語言中立版與目前介面語言的翻譯版。
- **主題跟隨系統**：主題按鈕改為三態循環（淺色 → 深色 → 跟隨系統 → 淺色），預設為「跟隨系統」，讀取 `prefers-color-scheme` 並即時監聽系統設定變化；使用者一旦手動選擇淺色或深色，就會固定下來、不再跟著系統走，選擇會存進 `localStorage`。

### 測試
- 新增三組回歸測試（開發用，未隨專案分發）：
  - `worker_regress.js`：驗證 `sha256Async()` 在 Worker 不存在、建構失敗、`postMessage` 失敗、Worker 回報錯誤、逾時、正常回應六種情境下都能算出正確雜湊。
  - `customsig_regress.js`：驗證自訂簽章的輸入驗證（空白名稱、非法 HEX、奇數長度、重複名稱）、`localStorage` 讀寫、以及最重要的一項——自訂簽章加入後真的會被 `resolveSignature()` 用來判定檔案格式，移除後也真的不再生效。
  - 既有的 `state_regress.js` 擴充了 JSON 匯出結構、差異比對核心邏輯（`buildByteDiff()`）、比較選取的先進先出邏輯、以及主題三態循環與跟隨系統的測試。
- 現有六組回歸測試（含瀏覽器內建的 `test.html`）全數重新跑過確認互不影響，共 100 餘項案例全過。

---

## v3.5 — 網址列狀態、已檢查持久化、復原刪除、空狀態引導

### 新增
- **檢視狀態存進網址列**：搜尋字串、chip 篩選、類型／重複／隱藏已檢查的勾選、排序欄位與方向、介面語言都會同步到 URL hash，可以把「目前這個檢視」的連結直接貼給同事。用 `history.replaceState` 而非直接改 `location.hash`，避免每動一個篩選就在瀏覽器歷史塞一筆。控制列新增「複製檢視連結」按鈕。**只記錄檢視條件，不含任何檔名或檔案內容**——檔案從來沒有離開瀏覽器，連結自然也帶不走。
- **已檢查標記的本機記憶（預設關閉）**：勾選「記住已檢查標記」後，標記會以**檔案內容的 SHA-256** 為索引存在瀏覽器的 localStorage，下次開啟時自動套用。刻意用內容雜湊而非檔名，換名字或換路徑的同一份檔案仍然認得出來，而且存下來的資料看不出原始檔名。只存雜湊值本身，不存檔名、大小、路徑。附「清除已存記錄」按鈕，勾選時畫面會顯示一段明確說明。**這是本工具唯一會在裝置上留下紀錄的功能，因此預設關閉，需要自己開啟。**
- **復原刪除**：誤按 ✕ 之後可用「復原刪除」按鈕或鍵盤 <kbd>u</kbd> 還原，最多回溯 20 筆，並且會**放回原本的索引位置**而不是接在清單最後。被刪除的項目在還留在復原堆疊裡時不會 revoke 其預覽 URL，否則復原後縮圖會顯示不出來。
- **匯出報告記錄檢查狀態與篩選條件**：CSV 與 TXT 都新增「檢查狀態」欄位，並在檔案開頭記錄匯出當下套用了哪些篩選、以及「已檢查 N / 總數」。做稽核紀錄時這很重要——一份只列 12 筆的報告，若沒寫清楚是套了「只看高風險」的結果，日後回頭看會誤以為當時整批只有 12 個檔案。
- **空狀態引導**：還沒拖入檔案時顯示說明與三個具體的試用建議（把 .png 改名成 .pdf 拖進來、丟一個含巨集的 .docm、整個下載資料夾拖進來）。
- 新增鍵盤快速鍵 <kbd>u</kbd>（復原刪除），已補進無障礙說明區塊。

### 修正
- **`saveReviewedSet()` 其實從來沒存進任何東西**：原本用 `Array.prototype.slice.call(set)` 把 Set 轉成陣列，但 Set 不是 array-like（沒有 `length`），這個寫法會永遠得到空陣列——也就是「看起來有存、實際上寫進去的是 `[]`」。已改用 `Array.from()`。這個 bug 是新加的狀態測試抓出來的，不會有任何錯誤訊息，只會靜靜地讓持久化功能完全失效。
- **`document.activeElement` 可能為 null 導致拋錯**：`toggleReviewed()`、`focusAdjacentRow()` 與 <kbd>r</kbd> 快捷鍵三處都直接存取 `document.activeElement.closest`，但該屬性在元素剛被移除、文件尚未取得焦點等情況下會是 null。已抽出共用的 `focusedRow()` 並在其中同時防範 null 與「節點沒有 closest 方法」兩種情況。

### 測試
- 新增第四組回歸測試（開發用，未隨專案分發），用最小 DOM 模擬驗證網址列狀態往返（含 `&`、`#`、`%`、空白、中文等特殊字元）、持久化的寫入／移除／重新載入套用、關閉後不再寫入、復原刪除的位置正確性，以及匯出篩選描述不含 undefined 破洞。上述兩個 bug 都是這組測試發現的。

---

## v3.4 — 鍵盤快速鍵、已檢查標記、chip 篩選、CSP、對比度修正

### 新增
- **鍵盤快速鍵**：<kbd>/</kbd> 跳到搜尋框、<kbd>Esc</kbd> 清空搜尋（再按一次離開輸入框）、<kbd>j</kbd>／<kbd>k</kbd> 在結果列之間上下移動焦點、<kbd>r</kbd> 標記目前這列為已檢查。在輸入框／文字區域內一律不攔截，也不會覆蓋帶 Ctrl／Cmd／Alt 的瀏覽器原生快捷鍵；燈箱開啟時交由燈箱自己處理。說明已加入無障礙區塊，三語言皆有。
- **「已檢查」標記**：每列新增 ✓ 按鈕（`aria-pressed` 正確反映狀態），標記後該列變淡並加上綠色左邊框。搭配控制列新增的「隱藏已檢查」勾選，可以把一大批檔案分次檢查完。用鍵盤連續標記時會自動把焦點交給遞補上來的那一列，不用每標一次重新找位置。摘要列會顯示「已檢查 N / 總數」的進度。
- **摘要 chip 變成可點擊的篩選器**：點「高風險 3」就只顯示那三筆，再點一次取消。計數為 0 的 chip 維持靜態顯示（點了只會得到空清單）。chip 篩選的優先權高於「只顯示可辨識類型」——使用者主動點了某個分類，就不該再被類型篩選擋掉。
- **拖放區在有結果後自動縮小**：從佔滿版面的區塊縮成一條細長的「再拖曳檔案或資料夾到這裡加入清單」，把版面讓給結果清單；按「清除全部」後會還原。
- **`Content-Security-Policy` meta 標籤**：`default-src 'none'` 起手，只放行同來源的 script/style、預覽需要的 `blob:`，並把 `connect-src` 直接設為 `'none'`（本工具不發任何網路請求）。這對一個安全用途的工具是很便宜的加固，也能防止日後不小心引入 CDN。

### 修正
- **淺色主題兩個顏色未達 WCAG AA**：實際計算後發現 `--match`（綠）對 `--panel-2` 背景只有 3.87:1、`--doc`（琥珀）只有 4.46:1，都低於一般大小文字的 4.5:1 門檻。已調整為 `#18783f` 與 `#8c5d08`，最差對比度分別提升到 4.88 與 5.03，色相幾乎不變。**兩套主題現在全部 7 個前景色對 3 種背景都達標**（深色最差 4.88、淺色最差 4.80）。順帶澄清：原本擔心的 `--muted` 灰階其實一直都是合格的（4.88～5.85）。
- **檔案讀取失敗不再靜默**：原本 `readBytes()` 失敗只是 catch 住，結果顯示成「空檔案或無法讀取」，使用者無從得知發生什麼事、也可能誤以為這個檔案檢查過沒問題。現在會保留錯誤訊息並轉成一則高風險說明，指出常見原因（掃描期間檔案被移動／刪除／重新命名，或磁碟機、網路磁碟暫時無法存取）並建議重新拖入。

---

## v3.3 — 手機版版面修正、動態效果擴充、無障礙說明、無 JavaScript 提示

### 修正
- **手機版預覽縮圖位置錯誤**：原本卡片版面把 `td.preview` 設成 `position:absolute` 浮在右上角，縮圖會脫離文件流、蓋住右側靠右對齊的「大小」與「修改時間」數值（檔名只有一行時特別明顯）。改用 CSS Grid 兩欄配置，縮圖固定在左欄並跨前兩列，有自己的欄位寬度，不再與任何內容重疊，也拿掉了原本靠 `padding-right` 硬留空間的權宜寫法。
- 修正 `styles.css` 章節編號在多次編輯後跳號（10/12/13）的問題，並同步更新檔案開頭的索引。

### 新增
- **更多 CSS 動態效果與互動**（全部純 CSS，不靠 JS 算時間軸）：
  - 表格列進場改為階梯式延遲（前 8 列各差 14ms），一批結果是依序刷入而不是整塊突然出現。
  - 高風險列與判定標籤加上非常輕微的呼吸光暈，**只跑三次就停**，避免變成持續閃爍的干擾源。
  - 掃描進度下方加了一條滑動斜紋，表示「正在處理但不知道還要多久」。
  - 拖放區的十六進位圖示緩慢上下浮動；摘要標籤（chip）改為彈入並支援 hover 微浮起。
  - 燈箱背景加上 `backdrop-filter` 模糊（不支援的瀏覽器會自動忽略，仍有半透明黑底）。
  - 主題切換時大面積色塊改為平滑過渡而非瞬間跳色；搜尋框聚焦加上柔和光暈。
  - 桌機版與觸控裝置分開處理：`@media (hover:hover)` 只在真的有滑鼠時套用 hover 效果，觸控裝置改用 `:active` 的按壓回饋。
  - 既有的 `prefers-reduced-motion` 覆蓋規則涵蓋所有新增動畫，系統開啟「減少動態效果」時全部自動關閉。
- **無障礙操作說明**：頁面新增可摺疊的「鍵盤操作與無障礙說明」區塊，說明 <kbd>Tab</kbd>／<kbd>Enter</kbd>／<kbd>Esc</kbd> 的行為、螢幕報讀器支援情況、動態效果與主題設定，並誠實列出已知限制（縮圖屬於巢狀互動元素）。三種語言皆已翻譯。
- **未啟用 JavaScript 時的提示**：新增 `<noscript>` 區塊，說明本工具需要 JavaScript 的原因（讀取位元組、比對簽章、計算雜湊都在瀏覽器端執行），並同時強調不會上傳檔案。因為 JS 關閉時 `i18n.js` 不會執行、無法動態翻譯，這裡是全專案唯一刻意將三種語言並列寫死的地方。`test.html` 也加了對應的簡短版本。

---

## v3.2 — 多語系（繁中／英文／日文）、測試覆蓋率補強、favicon

### 新增
- **多語系介面**：新增 `js/i18n.js`，支援繁體中文／English／日本語三種語言即時切換（頁首的 🌐 按鈕，zh → en → ja 循環），涵蓋介面文字、風險說明、格式名稱、CSV/TXT 匯出內容、對照表。語言選擇會存在 `localStorage`（純本機記憶，不涉及任何資料上傳），沒有存過的話依瀏覽器語言自動判斷，都對不上就預設英文。
  - `signatures.js`／`detectors.js` 內部判定邏輯用的格式名稱字串（例如 `"Word 97-2003 文件"`）維持不變，作為語言中立的內部識別碼；新增的 `formatName()` 只在**顯示時**依目前語言查表翻譯，找不到翻譯的名稱（多半是 PNG、JPEG 這類本來就是英文縮寫的格式）就直接顯示原始值。這樣可以在不動判定邏輯、不影響既有測試的前提下疊加多語系。
  - 修正了三處潛在的變數命名衝突：`app.js`／`detectors.js` 裡有幾個區域變數原本叫 `t`，會遮蔽新加入的全域翻譯函式 `t()`；已個別重新命名（`trailingResult`／`target`／`nextByte`／`txt`）。
- **測試覆蓋率補強**（`test.html`）：新增 6 組、22 個測試案例，補上先前完全沒有覆蓋的判定路徑——執行檔簽章（PE／ELF／shebang／LNK／Mach-O，含「偽裝成文件應標高風險」與「副檔名正確不算高風險」兩種情境）、`filenameRisks()`（雙重副檔名／RLO 文字方向控制字元／異常空白）、`sniffTextKind()`（SVG／HTML／JSON／PEM／登錄檔匯出，含「內嵌腳本應標高風險」情境）、Big5／GBK 舊式編碼、WebP／HEIC／AVIF、BMP／RIFF 標頭自述大小不符。目前總計 54 項測試。
- **favicon**：`assets/favicon.svg`，文件搭配放大鏡的向量圖示，配色沿用深色主題色票。純 SVG、不需要额外的圖檔轉檔工具。

### 修正
- `test.html` 的 `analyze()` 輔助函式改為同時回傳 `contentRisks()`／`filenameRisks()` 的結果，讓測試案例能檢查完整的風險判定，不只是格式偵測。
- 兩個 Node.js 回歸測試腳本（開發過程中用來驗證邏輯、未隨專案分發）補上載入 `i18n.js` 與必要的 `navigator`／`localStorage`／`document` 最小模擬，修正了因為 `detectors.js` 現在依賴 `t()`／`formatName()` 而導致測試環境跑不起來的問題。

---

## v3.1 — 無障礙、開發文件、瀏覽器內建測試、大檔案預覽保護

### 新增
- **鍵盤操作支援**：表格每一列都能用 <kbd>Tab</kbd> 移到、<kbd>Enter</kbd>／空白鍵展開明細（`role="button"` + `aria-expanded` + `aria-controls`）；縮圖（圖片/影片/音訊/PDF 的預覽入口）也能個別用鍵盤觸發。
- **燈箱無障礙化**：改為 `role="dialog"` + `aria-modal="true"`，開啟時把焦點移進去、記住原本的焦點、<kbd>Tab</kbd> 在對話框內循環（基本焦點陷阱），關閉時焦點還給原本點擊的縮圖。
- **`role`／`aria-live` 屬性**：高風險警示橫幅改為 `role="alert"`（緊急、會打斷朗讀），複製成功等提示訊息改為 `role="status"`（一般、不打斷）。
- **`README.md`**：說明怎麼開啟、資料夾結構、修改指引對照表、開發與測試方式、無障礙現況與已知限制。
- **`test.html`**：瀏覽器內建的輕量回歸測試頁，開啟即自動執行。內建最小可行的 ZIP／CFB／PNG 二進位產生器，當場合成測試檔案（不需要伺服器、不需要 Node、不需要事先準備測試檔），涵蓋 CFB 目錄解析、OOXML 巨集感知、附加資料／polyglot 偵測、PDF 主動掃描、簽章表完整性、SHA-256 對照 NIST 公開測試向量等案例。只載入 `utils.js`／`sha256.js`／`signatures.js`／`detectors.js` 四個純邏輯檔案，跟畫面渲染完全分開。
- **大檔案預覽保護**：新增 `PREVIEW_SIZE_LIMIT`（依種類分別設定圖片 30MB／影片 300MB／音訊 100MB／PDF 80MB 的門檻）。超過門檻的檔案不會自動建立預覽用的 Blob，縮圖改用虛線邊框標示「需要確認」，點開燈箱後會先看到「檔案過大，要不要仍然載入」的確認畫面，按下去才真的建立預覽並播放/顯示。

### 修正
- 清理 `css/styles.css` 裡未使用的 `.remove-btn`／`spinSlow` 死碼。
- 補上 `:focus-visible` 樣式，讓新增的鍵盤可操作元素（列、縮圖、燈箱關閉鈕、對照表折疊按鈕）在鍵盤導覽時有清楚一致的焦點外框。

---

## v3.0 — 多媒體預覽、行動裝置支援、專案拆分

### 新增
- **多媒體預覽**：圖片、影片（MP4/QuickTime/AVI/WebM）、音訊（WAV/MP3/FLAC/OGG/M4A）、PDF 都能在點擊縮圖後於燈箱（Lightbox）內直接預覽或播放。預覽一律用**偵測到的實際格式**包成正確 MIME 類型的 Blob，不受（可能造假的）原始副檔名影響。
- **手機／觸控裝置版面**：結果表格在寬度 720px 以下自動切換成卡片式版面；所有可點擊元素在小螢幕上放大到符合觸控目標建議尺寸；控制列、搜尋框、按鈕列改為可換行/堆疊。
- **複製開頭 HEX**：明細面板的位元組傾印旁新增「複製 HEX」按鈕，一鍵複製前 64 bytes 的十六進位字串。
- **頁尾參考資料（References）**：列出本工具引用或參考的官方規格與資料來源（ZIP/CFB/PDF 規格、SHA-256 標準、WebCrypto 文件、檔案簽章對照表等）。
- **CSS 動態效果**：表格列、明細面板、提示訊息（toast）、警示橫幅等改用 CSS `@keyframes` 做進場動畫；按鈕加上按壓回饋（`transform:scale`）；拖放區在拖曳中會有呼吸感的脈動提示。已加入 `prefers-reduced-motion` 覆蓋規則，尊重使用者的無障礙設定。

### 重構
- **專案拆成多檔案**，方便後續維護：
  ```
  index.html            結構
  css/styles.css        樣式（含分節註解）
  js/utils.js           共用小工具
  js/sha256.js          SHA-256 雜湊
  js/signatures.js      簽章表、分類資料（純資料）
  js/detectors.js       判定邏輯（ZIP/CFB/文字啟發式/風險判定）
  js/preview.js         縮圖與燈箱預覽
  js/reference-table.js 對照表資料與渲染
  js/app.js             狀態、掃描流程、表格渲染、事件綁定
  ```
  之前是單一 HTML 檔內嵌全部樣式與程式碼；拆分後每個檔案職責單一，新增格式或修改判定邏輯時通常只需要動 `signatures.js` 或 `detectors.js`，不會動到 UI 程式碼。載入方式仍是純 `<script src>`，沒有引入建置流程或模組打包工具。

---

## v2.2 — 巨集感知、CFB 目錄解析、附加資料偵測、PDF 主動掃描

### 新增
- **OOXML 巨集感知**：偵測到 `vbaProject.bin` 時，Word/Excel/PowerPoint 的建議副檔名會自動排除不允許巨集的版本（例如 `.docx` → 只建議 `.docm`），避免「含巨集卻標示相符」的誤判。
- **CFB／OLE2 複合文件目錄解析**：`.doc` / `.xls` / `.ppt` / `.msi` / `.msg` 開頭位元組完全相同，新增走訪 CFB 目錄鏈（FAT／DIFAT／目錄項目）取得內部串流名稱與 root CLSID，才分得出實際子格式；MSI 額外用其專屬的編碼字元集偵測。
- **結尾附加資料／polyglot 偵測**：依格式尋找結束標記（JPEG `FFD9`、PNG `IEND`、PDF `%%EOF`），標記後仍有大量資料就提示；並在非壓縮格式的尾端掃描 ZIP/RAR/7z 簽章，抓出常見的附加式 polyglot 檔案。
- **PDF 主動風險掃描**：掃描 `/JavaScript`、`/OpenAction`、`/Launch`、`/EmbeddedFile` 等關鍵物件，找到可執行動作時標示高風險，並附上「這項掃描的限制」說明（物件可能被放進壓縮串流而掃不到）。
- **重複檔案偵測**：SHA-256 算完後自動依雜湊值分組，表格顯示「重複 ×N」標籤，摘要列出可省下的空間，並可篩選「只看重複檔案」。
- **複製全部雜湊**：依目前篩選結果去重後一次複製多組 SHA-256。
- **渲染分頁**：表格預設只畫前 150 列，捲到底部可以「再顯示」或「全部顯示」，避免檔案數量很多時整頁卡頓。

---

## v2.1 — SHA-256、文字啟發式、更多容器格式、資料夾支援

### 新增
- **SHA-256 雜湊**：優先用瀏覽器內建 WebCrypto，不支援時退回純 JavaScript 實作（已用 Node.js `crypto` 模組做過區塊邊界與串流分塊的回歸測試）；雜湊旁附「在 VirusTotal 查詢」連結。
- **UTF-8／Big5／GBK 純文字啟發式判讀**：原本沒有固定簽章的純文字檔（無 BOM 的 `.txt`／`.json`／`.ps1` 等）會被標成「未知簽章」，改用 UTF-8 合法性檢查與可列印字元比例來判斷；UTF-8 驗證失敗時再嘗試 Big5／GBK 這類雙位元組編碼，避免台灣/中國舊系統匯出的文字檔被誤判。
- **更多容器格式**：ZIP/RAR/7z/GZIP/BZIP2/XZ、MP4/MOV/MKV/WebM/AVI/WAV/MP3/FLAC/OGG、PSD、SQLite、字型檔（TTF/OTF/WOFF/WOFF2）。
- **拖曳整個資料夾**：用 `DataTransferItem`／`webkitGetAsEntry` 遞迴走訪子資料夾，也支援「選擇資料夾」按鈕（`webkitdirectory`）。
- **點縮圖放大**、表格排序（點欄名）、檔名搜尋、CSV 匯出。
- **限制說明**：明確寫出這個工具能證明什麼（不是宣稱的格式）、不能證明什麼（完整性、安全性），並解釋 polyglot 檔案的概念。

### 重構
- **移除 JSZip 這個 CDN 依賴**：改成自己解析 ZIP 中央目錄（EOCD → Central Directory → 檔名清單），ODF/EPUB 的 `mimetype` 因規格要求是未壓縮的第一筆，直接讀取即可不需要解壓縮函式庫。
- 移除 Google Fonts，字型改用系統字型堆疊（含 PingFang TC／微軟正黑體）。這兩項讓整份工具成為真正不需要網路連線就能執行的單一 HTML 檔。

---

## v2.0 — 執行檔偵測

### 新增
- **執行檔簽章**：Windows PE（`MZ`）、ELF、Mach-O（四種位元序）、Java class、Shell/腳本 shebang（`#!`）、Windows 捷徑（LNK）、CAB、Android DEX、Debian/RPM 套件、Flash SWF。
- **雙重副檔名偵測**（例如 `發票.pdf.exe`）、Unicode 文字方向控制字元偵測（RLO 之類用來偽裝副檔名的手法）、異常大量空白偵測。
- **高風險獨立分級**：內容是執行檔但副檔名偽裝成文件／圖片時，跟單純的「副檔名不符」分開處理，整列標紅並在頁面頂端跳出警示橫幅。

---

## v1.x — 初版與介面打磨

- 初版：讀取檔案開頭位元組，比對 PNG／JPEG／PDF／Office 97-2003／RTF／BMP／TIFF／GIF 等常見簽章，跟副檔名比對「相符／不符」。
- 加入「副檔名不符不代表檔案異常」的說明區塊，列出手動改名、另存新檔、程式轉檔等常見原因。
- 深色／淺色模式切換（預設淺色）。
- 圖片縮圖預覽、單筆移除、「常見檔案簽章對照表」（預設收合）。
- 展開列可看到「依副檔名應為 vs 實際讀到內容」的逐位元組對照方塊，標示出從哪個位元組開始不一樣。
